{
    "discord": {
        "token": "Discord bot token",
        "id": "Discord bot ID",

		"owner": [ "Your user ID" ],
        "inviteCode": "Invite code to support server, e.g. turing"
    },

    "branding": {
        "noticeColor": "This is the color used for most embeds",
        "color": "#5765F2"
    },

    "noticeMetrics": "Whether metrics about usage, cool-down, guilds, users, etc. should be collected in the database",
    "metrics": true,

    "noticeDev": "Whether developer info should be logged",
    "dev": false,

    "clusters": "auto",
    "shardsPerCluster": 3,

    "channels": {
        "moderation": {
            "channel": "channel id",
            "guild": "guild id"
        },

        "error": {
            "channel": "channel id",
            "guild": "guild id"
        },

        "status": {
            "channel": "channel id",
            "guild": "guild id"
        }
    },

    "openAI": {
        "key": "OpenAI API key"
    },

    "replicate": {
        "key": "Replicate API key"
    },

    "ocr": {
        "key": "https://ocr.space API key"
    },

	"huggingFace": {
		"key": "HuggingFace session key"
	},

    "topgg": {
        "key": "top.gg webhook key"
    },

    "turing": {
        "key": "Turing API key @ https://github.com/TuringAI-Team/turing-ai-api",
        "captchas": {
            "turnstile": "Turing Turnstile verification key"
        },
        "urls": {
            "prod": "URL to the hosted Turing API"
        }
    },

    "gif": {
        "tenor": "Tenor API key"
    },

    "stableHorde": {
        "key": "Stable Horde API key"
    },

    "rabbitMQ": {
        "url": "URL to RabbitMQ instance"
    },

    "db": {
        "supabase": {
            "url": "Supabase URL",
            "key": {
                "service": "SupaBase service API key"
            },
            "collections": {
                "users": "users",
                "conversations": "conversations",
                "guilds": "guilds",
                "interactions": "interactions",
                "images": "images",
                "keys": "keys"
            }
        },
        "redis": {
            "url": "Redis URL",
            "password": "Redis password",
            "port": 12345
        }
    }
}